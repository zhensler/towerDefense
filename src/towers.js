// each element of array is a tower
// tower array contains [ID, name, range radius, dps]
// not sure how to store effect info, might have to hard code elsewhere
var towers = [
    [0, 'Standard', 5, 5],
    [1, 'Short', 3, 10],
    [2, 'Ranged', 7, 3]
];

export {towers};